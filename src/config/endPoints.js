const BASEURL = "http://65.0.93.119:4000";

const ENDPOINTS = {
  addUser: `${BASEURL}/adduser`,
  removeUser: `${BASEURL}/remove`,
  updateUser: `${BASEURL}/update`,
  fetchUsers: `${BASEURL}/fetchusers`,
};
export default ENDPOINTS;
